using System;

class Program
{
    static void Main()
    {
        string input = Console.ReadLine();
        int p = 0; //positions
        int len = input.Length; // length
        int jump = input[0];    //jumps
        if (jump > len)
        {
            Console.WriteLine("Fell off the dancefloor at {0}!");
        }
        else

    }
}
